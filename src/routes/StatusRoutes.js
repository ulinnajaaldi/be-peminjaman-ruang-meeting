// import { Router } from "express";
// import jwtAuth from "../middleware/jwtAuth.js";
// import {
//   createStatus,
//   deleteStatus,
//   getStatus,
//   getStatusById,
//   updateStatus,
//   updateStatusByAdmin,
// } from "../controllers/StatusController.js";

// export const StatusRouter = Router();

// StatusRouter.get("/", getStatus);
// StatusRouter.get("/:id", getStatusById);
// StatusRouter.post("/", jwtAuth, createStatus);
// StatusRouter.put("/:id", jwtAuth, updateStatus);
// StatusRouter.patch("/:id", jwtAuth, updateStatus);
// StatusRouter.delete("/:id", jwtAuth, deleteStatus);
// StatusRouter.patch("/:id/admin", jwtAuth, updateStatusByAdmin);
